import { Button } from '@davXionLabs/uilibrary/lib'

export default function App() {
  return (
    <div>
      <Button title='little button' onClick={()=>alert('hello')}>Hello Monorepo</Button>
    </div>
  )
}
